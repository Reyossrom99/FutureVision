
class Training:
    def __init__(self, proyect_id, input, is_training, is_trained, data, data_folder):
        self.proyect_id = proyect_id
        self.input = input
        self.is_training = is_training
        self.is_trained = is_trained
        self.data = data
        self.data_folder = data_folder
