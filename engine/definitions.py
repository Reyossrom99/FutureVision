
class Training:
    def __init__(self, project_id, input, data, data_folder, training_id):
        self.project_id = project_id
        self.input = input
        self.data = data
        self.data_folder = data_folder
        self.training_id = training_id
